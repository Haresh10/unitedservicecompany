import Input from "./input.component";
import Select from "./select.component";
import { Form, useForm } from "./useform.component";
const Controls = {
  Input,
  Select,
  Form,
  useForm,
};

export default Controls;
