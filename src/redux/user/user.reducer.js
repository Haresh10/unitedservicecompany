const INITIAL_STATE = {
  currentUser: null,
  errorMessage: undefined,
};
const UserReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default UserReducer;
